<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
        Grants the camera app write access to the temp capture file stored in
        the app's internal cache directory under camera_photos/.
        Authority: ${applicationId}.fileprovider
    -->
    <cache-path
        name="camera_photos"
        path="camera_photos/" />
</paths>
